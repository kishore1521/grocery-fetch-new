// Stub — this screen is never actually rendered.
// The scan tab uses a custom tabBarButton that navigates directly
// to /receipt/upload via router.push.
import { View } from 'react-native'
export default function ScanStub() {
  return <View />
}
